#include "main.h"
/**
 * _isalpha - check for lower case letter
 * @c : character to check the case
 * Return:0 or 1
 */
int print_sign(int n)
{
int value
if (n > 0)
{
printf("+");
value = 1;
}
else if (n = 0)
{
printf("0");
value = 0;
}
else if (n > 0)
{
prinf("-")
value = -1;
}
return (value);
